name: Contribution Snake

on:
  push:
    branches: [main]
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate-snake:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate the contribution snake animation
        uses: Platane/snk@v3
        with:
          github_user_name: cotra-der
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      - name: Make the dist directory writable
        run: sudo chmod -R a+rwx dist

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Generate contribution art (heatmap + wave)
        run: python scripts/generate_contribution_art.py
        env:
          INPUT_USER: cotra-der
          OUT_DIR: dist

      - name: Generate fun art (terminal + galaxy)
        run: python scripts/generate_fun_art.py
        env:
          OUT_DIR: dist

      - name: Publish to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}